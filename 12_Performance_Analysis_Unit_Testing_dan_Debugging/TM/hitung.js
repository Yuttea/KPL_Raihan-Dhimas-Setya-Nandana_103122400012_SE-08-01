// tambahkan export agar bisa digunakan di file hitung_test.js
export function tambahPengitung(terkini, jumlah) {
    // lebih ringkas dibuat langsung return hasil penjumlahan
    return terkini + jumlah;
}